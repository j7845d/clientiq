services:
  - type: web
    name: sales-power-up-suite
    env: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "cd backend && node dist/server.js"
    envVars:
      - key: NODE_ENV
        value: production
